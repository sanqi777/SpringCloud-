server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置Nacos地址
        #server-addr: 47.98.164.132:1111

management:
  endpoints:
    web:
      exposure:
        include: '*'
